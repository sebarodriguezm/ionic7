import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cl.app-compras.app',
  appName: 'app-compras',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
